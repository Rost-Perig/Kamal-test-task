import { Provider } from 'react-redux'
import { createContext } from 'react'

// export const OffsetContext = createContext()

// export const OffsetProvider = (children: React.ReactNode) => {
//     return (<OffsetContext.Provider)
// }
